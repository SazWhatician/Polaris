# Polaris backend — pinned for reproducibility.
# Regenerate with: pip-compile requirements.in (when you adopt pip-tools).

# --- Web framework ---
fastapi>=0.115.2,<1.0.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.18

# --- Config / validation ---
pydantic>=2.9.0
pydantic-settings>=2.6.0

# --- Logging / observability ---
structlog==24.4.0
setuptools>=70.0.0
email-validator>=2.0.0
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
opentelemetry-instrumentation-httpx==0.48b0
opentelemetry-exporter-otlp-proto-grpc==1.27.0

# --- Supabase & Auth ---
supabase>=2.11.0
pyjwt[crypto]>=2.10.1
firebase-admin>=6.5.0
google-cloud-firestore>=2.16.0

# --- HTTP client ---
httpx>=0.27.0

# --- Task queue (arq client; the worker image adds OCR deps) ---
arq==0.26.1
redis==5.0.8

# --- Image / PDF (kept in base so tests + page_extractor work without the OCR image) ---
pillow>=10.0.0
pypdfium2==4.30.0
numpy<2.0.0,>=1.26.0

# --- RAG stack ---
torch
langchain-text-splitters==0.3.2
sentence-transformers>=5.0.0
qdrant-client==1.12.1
groq==0.13.0
sse-starlette==2.1.3
langgraph==0.3.34
langgraph-checkpoint==2.1.2
flashrank>=0.2.9
